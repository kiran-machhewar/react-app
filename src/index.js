import React from 'react';
import ReactDOM from 'react-dom';
//Create the component
const App = ()=>{
    return <div>hello World</div>
}
//Add the component on the dom

ReactDOM.render(<App/>,document.querySelector('.container'));
